module.exports = {
  presets: [
    "@babel/preset-env", // Responsável por alterar as propriedades do js que o navegador não entende
    "@babel/preset-react", // Responsável por alterar as propriedades do react que o navegador não entende
  ]
}